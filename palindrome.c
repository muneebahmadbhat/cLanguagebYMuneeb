#include<stdio.h>
#include<stdlib.h>
int isPalindrome (int num){
    
return ;
}

int main (){
    int number;
    printf("Enter a number to check whether if it's a palindrome or not\n");
    scanf("%d",&number);

    if (isPalindrome(number)){
        printf("This number is a palindrome\n");
    }else   
    {
        printf("This number is not a palindrome\n");
    }
    
    return 0;
}